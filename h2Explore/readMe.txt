when creating the packages inside the com.tts.main package, 
make sure you click on that package to create a new file
it will create a new package that starts with name of com, just add .domain
.controller, and .repository.

